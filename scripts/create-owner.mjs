import mysql from 'mysql2/promise';import bcrypt from 'bcryptjs';
const req=['OWNER_EMAIL','OWNER_PASSWORD'];for(const x of req)if(!process.env[x]){console.error(`Ontbreekt in omgevingsvariabelen: ${x}`);process.exit(1)}
const db=await mysql.createConnection({host:process.env.DB_HOST||'127.0.0.1',port:Number(process.env.DB_PORT||3306),user:process.env.DB_USER||'root',password:process.env.DB_PASSWORD||'',database:process.env.DB_NAME||'klushulp_noord'});const hash=await bcrypt.hash(process.env.OWNER_PASSWORD,12);await db.execute(`INSERT INTO users(name,email,password_hash,active) VALUES('Jochem Visser',?,?,1) ON DUPLICATE KEY UPDATE password_hash=VALUES(password_hash),active=1`,[process.env.OWNER_EMAIL,hash]);const [u]=await db.execute('SELECT id FROM users WHERE email=?',[process.env.OWNER_EMAIL]);await db.execute('INSERT IGNORE INTO user_roles(user_id,role_id) VALUES(?,1)',[u[0].id]);console.log('Eigenaar-account is aangemaakt/bijgewerkt:',process.env.OWNER_EMAIL);await db.end();
