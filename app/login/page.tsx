'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const [error,setError]=useState('');const r=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(res.ok)r.push('/');else setError((await res.json()).error)}return <main className="login"><form onSubmit={submit} className="panel loginbox"><div className="brandMark">KHN</div><h1>KlusHulp Noord</h1><p>Administratie v6</p><label>E-mailadres<input name="email" type="email" required autoFocus/></label><label>Wachtwoord<input name="password" type="password" required/></label>{error&&<div className="error">{error}</div>}<button>Inloggen</button></form></main>}
